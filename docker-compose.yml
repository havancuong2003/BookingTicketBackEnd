services:
  dev-db:
    image: postgres:14
    ports:
      - 5432:5432
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 123456
      POSTGRES_DB: booking_ticket
    networks:
      - freecodecamp

networks:
  freecodecamp:
